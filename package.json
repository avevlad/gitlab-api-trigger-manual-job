{
  "name": "gitlab-api-trigger-manual-job",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "NODE_NO_WARNINGS=1 npx tsc-watch --onSuccess 'node  --experimental-specifier-resolution=node --loader ./dist/main.js'",
    "gitlab_find_pipile": "npx tsc && node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^14.14.6",
    "tsc-watch": "^4.2.9",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "@gitbeaker/cli": "^25.2.0",
    "@gitbeaker/node": "^25.2.0"
  }
}
